# Infra Scripts

Utility scripts and bootstrap assets for local infrastructure.

- Kafka topics: `infra/kafka/init-topics.sh` via `kafka-init` compose service.
- PostgreSQL schema and seed data: `infra/postgres/init/`.
- Grafana provisioning: `infra/grafana/provisioning/` and `infra/grafana/dashboards/`.
